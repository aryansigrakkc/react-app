
import React from "react";
import ReactDOM from "react-dom";

const heading=<>
<h1>hello everyone this is react code</h1>
</>
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading)